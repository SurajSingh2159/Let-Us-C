#include <stdio.h>
#include <conio.h>
void main()
{
    int a, b;
    printf("\nEnter the marks of main subject 'A' and Subsidiary subject 'B'");
    scanf("%d%d", &a, &b);
    if (a >= 55 && b >= 45)
        printf("\nhe has passed the test\n");
    else if ((a == 55 && b >= ss) || (a >= 45))
        printf("\nhe has passed the test with condition apply\n");
    else if (a >= 65 && b < 45)
        printf("\nhe is allowed to reappear in an examination in B to qualify\n");
    else if (a < 45 && b < 44)
        printf("\nhe fails in the test\n");
}
